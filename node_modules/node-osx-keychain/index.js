const path = require('path');
const fs = require('mz/fs');

const KeyChain = require('./keychain');

const a = async () => {
  // Import Key
  //await fs.unlink('chain/hello.chain');
  //const chain = await KeyChain.create(path.resolve('chain/hello.chain'), '111111');
  //await chain.importP12(path.resolve('keys2.p12'), '111111');

  console.log(await KeyChain.getList());

  const chain = new KeyChain('/Users/r/Library/Keychains/login_renamed_1.keychain-db', '1');
  console.log(await chain.getItemsNames());

  // await KeyChain.getList() // find keychains list in system
};

module.exports = KeyChain;